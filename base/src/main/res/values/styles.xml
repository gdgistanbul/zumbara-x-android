<resources>

    <!-- Base application theme. -->
    <style name="AppThemeLight" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryText">@color/colorPrimaryTextLightTheme</item>
        <item name="colorSecondaryText">@color/colorSecondaryTextLightTheme</item>
        <item name="colorPrimaryDetailText">@color/colorPrimaryDetailTextLightTheme</item>
    </style>

    <style name="Widget.ProjectX" parent="@android:style/Widget" />

    <style name="Widget.ProjectX.CardItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/margin_small</item>
        <item name="android:layout_marginLeft">@dimen/margin_normal</item>
        <item name="android:layout_marginRight">@dimen/margin_normal</item>
    </style>

    <style name="Widget.ProjectX.RatioStarStyle">
        <item name="android:layout_width">18dp</item>
        <item name="android:layout_height">18dp</item>
        <item name="android:src">@android:drawable/btn_star</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

</resources>
